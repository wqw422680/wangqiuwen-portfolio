{
  "name": "xirui-portfolio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && node scripts/prepare-sites.mjs",
    "preview": "vite preview --host 0.0.0.0"
  }
}
